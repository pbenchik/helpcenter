---
icon: bookmark
---

# Настройка- готовая интеграция

## Настройка- готовая интеграция

Это инструкция по подключению партнёрской программы через готовый шаблон. Она помогает быстро настроить передачу событий в Chatterfy.

### Как это работает

В готовых интеграциях основные настройки уже подготовлены системой. Не нужно вручную создавать postback-ссылки и настраивать параметры с нуля.

С помощью готовой интеграции можно:

* подключить готовую интеграцию без ручной сборки postback-ссылок
* настроить передачу событий из партнёрской программы в Chatterfy
* связать события с пользователями через `clickid`
* добавить регистрационную ссылку для выдачи пользователям

В этой инструкции рассматривается пример подключения `PocketOption RevShare [v2] NEW`.

### Как настроить готовую интеграцию

1. Откройте нужную интеграцию в `Tracker → Integrations`.
2. Найдите `PocketOption RevShare [v2] NEW`.
3. Нажмите `Connect +`.
4. Укажите `Click ID Parameter`.
5. Проверьте созданные конфигурации событий и `Postback URL`.
6. Скопируйте `Postback URL` и добавьте его в кабинет партнёрской программы.
7. Добавьте регистрационную ссылку в `Registration Links`.
8. Сделайте тестовую регистрацию и проверьте события в `Tracker → Logs → Postback logs`.

### Укажите параметр clickid

Первый шаг — указать `Click ID Parameter`. Обычно используется параметр `click_id`.

![Пример настройки clickid](<../../../.gitbook/assets/Frame 2193.png>)

### Проверьте конфигурации событий

После выбора `clickid` система автоматически создаёт конфигурации событий и `Postback URL`.

![Пример созданных конфигураций](<../../../.gitbook/assets/unknown (130).png>)

#### Какие события доступны

В интеграции доступны события:

* `Registration (registration)` — регистрация пользователя
* `Email Confirmation (emailconf)` — подтверждение email
* `First Deposit (sale)` — первый депозит
* `Re-Deposit (resale)` — повторный депозит
* `Withdrawal (withdraw)` — вывод средств
* `Commission (Revshare)` — ревшарная комиссия

Для каждого события система генерирует свой `Postback URL`. Его нужно использовать в настройках партнёрской программы.

### Настройте postback в партнёрской программе

Скопируйте `Postback URL` из интеграции и добавьте его в кабинет партнёрской программы.

![Окно интеграции](<../../../.gitbook/assets/image (104).png>)

![Окно настройки в партнёрской программе](<../../../.gitbook/assets/Frame 2192.png>)

#### Что уже настроено в готовой интеграции

В готовой интеграции уже настроены:

* postback-параметры
* события, которые будут передаваться в систему
* готовые postback-ссылки

Вам остаётся:

* скопировать postback-ссылки
* добавить их в настройки партнёрской программы
* проверить соответствие параметров

### Как настроить postback в PocketOption

Для каждого события необходимо создать отдельный postback. Процесс настройки одинаковый.

1. Откройте раздел Постбеки в кабинете PocketPartners.
2. Выберите тип постбека: `Global` или `Campaign`.
3. Выберите событие.
4. Вставьте соответствующий `Postback URL` из Chatterfy.
5. Сопоставьте параметры события.

![Скриншот 1](<../../../.gitbook/assets/unknown (132).png>)

![Скриншот 2](<../../../.gitbook/assets/unknown (134).png>)

![Скриншот 3](<../../../.gitbook/assets/unknown (135).png>)

![Скриншот 4](<../../../.gitbook/assets/unknown (136).png>)

![Скриншот 5](<../../../.gitbook/assets/unknown (137).png>)

![Скриншот 6](<../../../.gitbook/assets/unknown (133).png>)

#### Важный параметр

* `clickid → CLICK_ID`

### Кастомные поля

Внутри postback используются дополнительные параметры, которые автоматически создают Custom Fields в CRM.

![Пример кастомных полей](<../../../.gitbook/assets/unknown (131).png>)

Например:

* `fields.trader_id`

Это означает:

* система создаст поле `trader_id`
* в это поле будет записываться ID клиента из партнёрской программы

### Дополнительные параметры

При необходимости можно включить дополнительные параметры, например:

* `site_id`
* `trader_id`
* `cid`
* `ac`
* `country`
* `sub_id1 ... sub_id5`
* `promo`
* `device_type`
* `os_version`
* `browser`
* `link_type`
* `date_time`
* `sumdep`
* `wdr_sum`
* `status`

### Добавьте регистрационную ссылку

После настройки postback необходимо добавить регистрационную ссылку в `Registration Links`.

![Форма добавления ссылки](<../../../.gitbook/assets/unknown (138).png>)

![Готовый пример заполнения](<../../../.gitbook/assets/unknown (139).png>)

#### Как это работает

Заполните поля:

* `Link name` — внутреннее название ссылки
* `Base URL` — партнёрская ссылка

После этого система автоматически сформирует `Final URL with Parameters`.

Пример:

`https://u3.shortink.io/smart/mByVsLe71bbFFH?click\_id=\{{tracker.clickid\}}`

### Проверка после настройки

После завершения настройки рекомендуется:

1. совершить тестовую регистрацию
2. дождаться отправки postback
3. проверить события в `Tracker → Logs → Postback logs`

Если события отображаются в логах без ошибок, интеграция настроена корректно.

### Notes

* Если `clickid` не передаётся в ссылке регистрации, система не сможет связать событие с конкретным пользователем.
* Тогда события не будут записываться в Tracker корректно.

### Где использовать готовую интеграцию

Готовая интеграция удобна, когда нужно:

* быстро подключить поддерживаемую партнёрскую программу
* избежать ручной сборки postback-ссылок
* связать регистрации и депозиты с пользователями через `clickid`
* автоматически получать события и работать с ними в Tracker

{% include "../../../../.gitbook/includes/start-i-obzoro-produktearkhi....md" %}
